# 2026-07-30

## 锁定/解锁编辑区功能
- 在 top-bar 的 statusIcon (💾) 左侧添加了 lockIcon (🔐/🔒)
- 点击切换 textarea 的 readOnly 属性
  - 🔐 默认态：可编辑
  - 🔒 锁定态：只读（可滚动/选择/复制，不可编辑）
- 锁定态添加 `.locked` CSS class（降低透明度视觉反馈）
- keydown 和 input 事件添加 `isLocked` 守卫防止意外写入
- 锁图标从 🔐 改为 🔓（开锁）/🔒（锁定），更直观
- 编辑区鼠标悬浮光标改为 ✍️ emoji（SVG data URL 自定义 cursor）
- 替换为 JS 驱动的自定义光标（隐藏原生 cursor，div 模拟跟随鼠标）
- 滚动方向动态光标：向下滚 👇、向上滚 👆、停止 500ms 恢复 ✍️，带 scale 弹性动效
- 锁定态不显示自定义光标
- 最终方案：CSS 原生 cursor: url(SVG) 显示 ✍️，JS 轻量 div 仅滚动瞬间弹出 👇👆（500ms 自消）
- 滚动方向改为显示在 statusIcon（💾→👇/👆→💾），不再用 floating div
- 点击 💾 复制全文（📋），而非 URL
- 修复 meta 过期警告：增加 `mobile-web-app-capable`
- icon 对齐优化：20x20 固定占位 + flex 居中
- 云端同步方案规划并落地前端 UI：状态点点击弹出同步面板，ID 输入 + ⬆️⬇️ 按钮，Worker 端预留空实现
- 同步 UI 重构：去掉弹窗，全部控件收入底部 footer（🕐 ⬆️ 🟢 ⬇️ 🗝️）
- ID 输入改为内联编辑（点击 🗝️ 切换），最大 16 字符，支持 UTF-8/emoji
- 上传下载过程中按钮禁用（pointer-events: none + 半透明），绿点反馈状态
- 编写 `cloud-sync-deploy.md` 部署指南：Worker 代码、KV 配置、前端接入完整步骤
